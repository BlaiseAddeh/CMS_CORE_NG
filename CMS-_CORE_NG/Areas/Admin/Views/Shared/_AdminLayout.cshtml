<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>CMS Application</title>
    <meta name="description" content="CMS Application build with Angular 12 and ASP.NET Core 5" />
    <meta name="author" content="Techhowdy" />
    <!-- Favicon icon -->
    <link href="~/favicon.ico" icon="icon" />

    @await Html.PartialAsync("_ApplicationHeaderStylesPartial")


    @RenderSection("Styles", required: false)
</head>
<body>

    <!-- ================================================== -->
    <!-- Main wrapper -->
    <!-- ================================================== -->

    <div class="wrapper">

        @await Html.PartialAsync("_SidebarLayout")

        <div id="content">

            @await Html.PartialAsync("_TopbarLayout")

            @RenderBody()
        </div>

    </div>

    @await Html.PartialAsync("_FooterLayout")

    @await Html.PartialAsync("_UIScriptsPartial")

    @RenderSection("Scripts", required: false)


</body>
</html>